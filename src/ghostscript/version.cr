module Ghostscript
  # Version du shard `ghostscript` (Crystal). Distincte de la
  # version du binaire `gs` installé sur la machine, accessible via
  # `Ghostscript.version`.
  VERSION = "0.1.0"
end
